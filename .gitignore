# Build output
bin/
obj/
[Bb]in/
[Oo]bj/
artifacts/

# IDEs
.vs/
.idea/
*.user
*.suo

# Test / coverage
[Tt]est[Rr]esult*/
coverage*.json
*.trx

# Deploy build artifacts (generated by deploy/publish.sh / CI)
deploy/out/
deploy/dist/
deploy/*.tar.gz

# ── PepperMill custody data — NEVER commit peppers or keys ──
peppers/
peppers-dev/
*.pepper
audit.log
# local-only secrets overrides
appsettings*.local.json
# container env file — holds the storage key + credential
.env
