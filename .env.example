# Copy to .env and fill in real values. .env is gitignored — never commit it.
#
# 32-byte AES-256 master key that encrypts peppers at rest, base64-encoded.
# Generate one with:   head -c 32 /dev/urandom | base64
PEPPERMILL_STORAGE_KEY=

# Tenant credentials are NOT set here — each tenant establishes its own during
# enrollment. To allow enrollment, allowlist your servers' callback hosts in
# docker-compose.yml (PepperMill__CallbackAllowedHosts__0, __1, ...).
